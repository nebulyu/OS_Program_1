踩过的坑

在处理R时不要分别输出,这样导致本地和服务器逻辑不同

注意此处不可直接申请动态字符数组,初始值赋为""
这个指向静态地址的指针会导致strcat时出现段错误

c99 不包含bool
需要#include <stdbool.h>

直接kill后有时间延迟,sleep后检验

lsof i : 然后杀掉进程
端口号不会自动结束进程

不知为何本地模式无法调用线程,服务器却可以
IO对齐后就没问题了

注意初始化所有的指针
不然很容易野

